- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim/init.vim: nvim
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: oh-my-zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
